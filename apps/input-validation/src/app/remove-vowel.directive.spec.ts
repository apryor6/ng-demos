import { RemoveVowelDirective } from './remove-vowel.directive';

describe('RemoveVowelDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoveVowelDirective();
    expect(directive).toBeTruthy();
  });
});
